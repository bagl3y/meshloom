MESHLOOM_HTTP_PORT=8123
MESHLOOM_DATA_PATH=/opt/docker/meshloom/data

MESHCORE_DATABASE_PATH=data/meshcore.db
MESHCORE_DISABLE_BOTS=false
# Fallback VAPID JWT subject when Settings → Notifications / app_settings.vapid_subject is empty.
MESHCORE_VAPID_SUBJECT=mailto:you@example.com
# New empty databases join Meshloom Community. Set 0 to seed opted out. Existing DBs are never flipped.
# MESHLOOM_COMMUNITY=0
# MESHLOOM_COMMUNITY_IATA=CDG
# MESHLOOM_COMMUNITY_LOCKED=0
